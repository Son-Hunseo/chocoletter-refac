apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: chocoletter-after-route
  namespace: chocoletter
spec:
  parentRefs:
    - name: web-gateway
      namespace: gateway # Gateway가 있는 ns
      sectionName: http-chocoletter-after
#    - name: web-gateway
#      namespace: gateway # Gateway가 있는 ns
#      sectionName: https-chocoletter-after
  hostnames:
    - chocoletter-after.sonhs.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: chocoletter-client-after-service
          port: 80