apiVersion: v1
kind: Service
metadata:
  name: chocoletter-server-service
spec:
  type: NodePort
  selector:
    app: chocoletter-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080